# NVIDIA Nemotron Telegram Bot

Telegram-бот на базе NVIDIA Nemotron 3 Ultra с отображением процесса рассуждений.

## Возможности
- ��� Показывает процесс «думания» поэтапно
- ��� Разделяет рассуждения и итоговый ответ
- ��� Streaming-режим (по токенам)
- ��� Работает через официальное NVIDIA API

## Установка

```bash
# 1. Клонируйте/создайте папку проекта
cd nvidia_telegram_bot

# 2. Создайте виртуальное окружение
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Настройте переменные окружения
cp .env.example .env
# Отредактируйте .env и вставьте ваши токены
```

## Получение токенов

### Telegram Bot Token
1. Напишите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

### NVIDIA API Key
1. Зарегистрируйтесь на [build.nvidia.com](https://build.nvidia.com/)
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Скопируйте в `.env`

## Запуск

```bash
python main.py
```

## Как это работает

1. Пользователь пишет вопрос
2. Бот отправляет «���� Думаю...»
3. В процессе генерации бот обновляет сообщение, показывая накопленные рассуждения
4. Когда модель переходит к ответу (обнаруживает маркеры вроде ``), бот пишет «���� Рассуждаю...»
5. В финале показывает полные рассуждения и ответ в одном сообщении с HTML-разметкой

## Структура проекта
```
nvidia_telegram_bot/
├── main.py          # Основной код бота
├── requirements.txt # Зависимости
├── .env.example     # Пример конфигурации
��── .env             # Ваши секреты (не коммитить в git!)
```